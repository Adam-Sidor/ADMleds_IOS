import SwiftUI

struct GeneralSetView: View {
    var body: some View {
        List {
            Text("There will be a General Settings view here!")
        }
        .navigationTitle("General Settings")
    }
}

#Preview {
    GeneralSetView()
}
